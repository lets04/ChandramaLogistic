# Brevo (email) - required for contact form and chatbot lead capture
BREVO_API_KEY=

# Local development with API routes (contact form):
#   npm run dev:api
# Plain `npm run dev` works for the chatbot (no API needed).
# For the contact form in local dev, use `npm run dev:api`.
